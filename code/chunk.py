# Il faudrait que nos objets chunk contiennent plusieurs trucs : les données audio,
# les donneés signal (beat position, tonalité), les metadata (nom de la track d'où est tiré le chunk...),
#et le feature vector

# getters for audio, signal metadata, other metadata ...

class chunk :
	